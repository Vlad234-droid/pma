{
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "ignoreChanges": ["**/*.test.{ts,tsx}", "**/*.md"],
  "command": {
    "publish": {
      "allowBranch": ["master"],
      "conventionalCommits": true,
      "message": "chore(release): publish",
      "exact": true,
      "push": true,
      "verifyAccess": false,
      "yes": true,
      "logLevel": "verbose"
    }
  }
}
