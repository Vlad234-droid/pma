{
  "name": "@pma-common/connector-utils",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "files": [
    "build"
  ],
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "clean": "rimraf build coverage .eslintcache",
    "clean:npm": "rimraf node_modules",
    "clean:all": "yarn clean && yarn clean:npm",
    "build:prod": "tsc -p ."
  },
  "dependencies": {
    "@energon-connectors/core": "^0.11.2",
    "@energon/type-utils": "^0.11.0",
    "cli-highlight": "^2.1.11"
  },
  "devDependencies": {
    "typescript": "^4.1.3"
  }
}
