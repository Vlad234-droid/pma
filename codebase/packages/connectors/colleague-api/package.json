{
  "name": "@pma-connectors/colleague-api",
  "version": "0.1.0",
  "license": "MIT",
  "files": [
    "build"
  ],
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "clean": "rimraf build coverage .eslintcache",
    "clean:npm": "rimraf node_modules",
    "clean:all": "yarn clean && yarn clean:npm",
    "build:prod": "tsc -p ."
  },
  "dependencies": {
    "@energon-connectors/core": "^0.8.7",
    "@energon/rest-api-consumer": "^0.7.7",
    "@energon/rest-api-definition": "^0.5.7",
    "@pma-common/connector-utils": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^4.1.3"
  }
}
