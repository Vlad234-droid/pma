# must be unique in a given SonarQube instance
sonar.projectKey=pma
#sonar.projectBaseDir=usr/src

# --- optional properties ---

# defaults to project key
#sonar.projectName=My project
# defaults to 'not provided'
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=src
sonar.exclusions=**/*test.tsx,src/test-utils.tsx,src/**/index.tsx,src/**/index.jsx,./coverage/**/*

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

#----- Default SonarQube server
sonar.host.url=http://localhost:9000

# --- test coverage
sonar.tests=src
sonar.test.inclusions=**/*test.tsx
sonar.typescript.lcov.reportPaths=coverage/lcov.info
sonar.login=a7758023510cef93fce9521a7bc68a092fffb326
#sonar.typescript.tslint.reportPaths=report-lint.json
