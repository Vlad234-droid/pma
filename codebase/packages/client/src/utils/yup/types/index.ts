export enum VALIDATION_RULE_MODELER {
  MIN_LENGTH = 'minLength',
  MAX_LENGTH = 'maxLength',
  REQUIRED = 'required',
}

export enum VALIDATION_RULE {
  MIN_LENGTH = 'min',
  MAX_LENGTH = 'max',
  REQUIRED = 'required',
}

export enum FORM_TYPE_MODELER {
  SELECT = 'select',
}
